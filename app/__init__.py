# This file marks the 'app' directory as a Python package.
# In PHP/Laravel you don't need this — PHP uses autoloading (PSR-4).
# In Python, every directory that should be importable MUST have __init__.py.
# It can be empty. Its presence is what matters.
